# Commercial S10 – Exposure Repositioning Control SSOT

## Decision

S10 is integrated as an offline, disabled design and evidence package. The
public North Star is a consensual adult exposure experience: a person chooses
to be seen, controls the audience and destinations, and can withdraw before
the separately confirmed final action. The internal product name remains
`TU1NZ Adult Publishing`; `Seen By Choice` is only the provisional external
name.

The Control decision is `NO_GO_PUBLIC_BRAND_ACTIVATION`. This record does not
authorize a public rename, domain purchase, handle reservation, server sync,
real participant, adult-media flow, identity/AVS flow, payment, publishing,
controlled beta or production.

## Exact application binding

The reviewed application state is:

- repository: `Tausendunde1nz/adult-publishing-core`
- branch: `main`
- commit: `cc004c27ce52f4c5df8989ec8c166f6247117aa8`
- tree: `7f911127765a167e750d862500ccc1c49587594b`

The source contract, central public-copy SSOT, brand research and three review
documents are bound by SHA-256 in the Control manifest. Any application or
hash drift invalidates this evidence.

## Prepared but inactive

The application contains a central bilingual copy/design SSOT, a script-free
and form-free SFW preview landing page, a deterministic button-first bot
persona, twelve weeks of SFW content inventory, opt-in nurture copy, opaque
referrals, aggregate-only analytics and synthetic future experience modes.

The modes are deliberately not a live Adult workflow. The contract keeps real
media, identity documents, AVS, payments, external publishing and real users
closed. Consent preview and payment boundaries are design constraints only.

## Brand and legal gates

Twenty-five candidate names were scored. Preliminary RDAP and open-web checks
are discovery evidence, not registration, availability or legal clearance.
Before any public change, the operator must obtain a manual DPMA/EUIPO/WIPO
similarity search, legal trademark clearance, domain ownership, exact social
handle ownership, public rename approval and approved imprint/privacy copy.

The address research does not authorize replacing legally required identity or
service-address information. A bare forwarding address or c/o construct is not
treated as cleared. Any address arrangement requires German legal review and a
genuine, usable establishment or service-address basis.

## Runtime boundary

This Control change contains no runtime script, unit, nginx configuration,
credential, provider call or server action. It does not contact the server and
does not alter S7, S8 or S9. Rollback is limited to reverting this Control
documentation merge; there is no server delta to undo.

After merge, a new separately versioned post-merge Control binding is required
before a later activation plan may even be evaluated. The later activation
must still stop at every hard human gate.
